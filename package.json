{
  "name": "logicore-root",
  "version": "1.0.0",
  "description": "LogiCore Mono-repo management",
  "scripts": {
    "start": "bash demo_startup.sh",
    "stop": "fuser -k 3000/tcp 3001/tcp 3002/tcp 3003/tcp 3004/tcp || true",
    "install-all": "npm install --prefix backend && npm install --prefix frontend-manager && npm install --prefix frontend-driver && npm install --prefix frontend-supplier && npm install --prefix frontend-gate",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend-manager",
    "migrate": "npm run migrate --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
